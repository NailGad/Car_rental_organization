# ✅ Этап 2 завершен: Паттерны проектирования и бизнес-логика

## 🎯 Что реализовано:

### 1. **Паттерн Strategy (Стратегия)** - Расчет стоимости

Создана система стратегий для расчета стоимости аренды:

#### **PricingStrategy** (базовый интерфейс)
- `calculateCost()` - расчет стоимости
- `getName()` - название стратегии
- `getDescription()` - описание стратегии

#### **Конкретные стратегии:**

1. **DailyPricingStrategy** - Расчет по дням
   - Базовая цена × количество дней
   - Используется для аренды менее 7 дней

2. **WeeklyPricingStrategy** - Расчет по неделям
   - Базовая цена × недели × 0.9 (скидка 10%)
   - Автоматически применяется для аренды ≥ 7 дней

3. **MonthlyPricingStrategy** - Расчет по месяцам
   - Базовая цена × месяцы × 0.8 (скидка 20%)
   - Автоматически применяется для аренды ≥ 30 дней
   - Для аренды 7-29 дней применяет недельную скидку

4. **FinePricingStrategy** - Расчет штрафа
   - Базовая цена × коэффициент (1.5) × дни просрочки
   - Используется при расчете штрафов за просрочку возврата

---

### 2. **Паттерн Observer (Наблюдатель)** - Обновление статуса

Реализована система автоматического обновления статуса автомобилей:

#### **ICarStatusObserver** (интерфейс наблюдателя)
- `onCarStatusChanged()` - метод уведомления об изменении статуса

#### **CarStatusObserver** (конкретный наблюдатель)
- Автоматически обновляет статус автомобиля в базе данных
- Вызывается при создании/завершении аренды

#### **CarStatusSubject** (субъект)
- `attachObserver()` - подписка наблюдателя
- `detachObserver()` - отписка наблюдателя
- `notifyStatusChanged()` - уведомление всех наблюдателей

**Как работает:**
1. При создании аренды → статус автомобиля меняется на "В аренде"
2. При завершении аренды → статус автомобиля меняется на "Доступен"
3. Все изменения автоматически сохраняются в БД

---

### 3. **RentalService** - Бизнес-логика аренды

Сервис для управления арендами:

#### **Основные методы:**
- `createRental()` - создание новой аренды
  - Проверяет доступность автомобиля
  - Создает запись в БД
  - Автоматически обновляет статус через Observer

- `completeRental()` - завершение аренды
  - Устанавливает фактическую дату возврата
  - Автоматически обновляет статус через Observer

- `calculateFine()` - расчет штрафа за просрочку
  - Вычисляет дни просрочки
  - Рассчитывает сумму штрафа

- `applyFine()` - применение штрафа
  - Сохраняет штраф в БД

- `isCarAvailable()` - проверка доступности
  - Проверяет статус автомобиля
  - Проверяет пересечение с существующими арендами

- `getActiveRentals()` - получение активных аренд
- `getUserRentals()` - получение аренд пользователя

---

### 4. **PricingCalculator** - Калькулятор стоимости

Удобный интерфейс для работы со стратегиями:

#### **Основные методы:**
- `setStrategy()` - установка стратегии вручную
- `calculateRentalCost()` - расчет стоимости аренды
- `selectOptimalStrategy()` - автоматический выбор оптимальной стратегии
  - < 7 дней → DailyPricingStrategy
  - 7-29 дней → WeeklyPricingStrategy
  - ≥ 30 дней → MonthlyPricingStrategy

- `getCalculationInfo()` - получение детальной информации о расчете

---

### 5. **ReportManager** - Менеджер отчетов

Система генерации отчетов и статистики:

#### **RevenueReport** (структура отчета)
- `totalRevenue` - общий доход
- `totalFines` - доход от штрафов
- `totalRentals` - количество аренд
- `activeRentals` - активные аренды
- `averageRentalDuration` - средняя длительность
- `fleetUtilization` - загруженность парка (%)

#### **Основные методы:**
- `generateRevenueReport()` - генерация отчета за период
- `getCarStatistics()` - статистика по автомобилям
- `getPopularCars()` - топ популярных автомобилей
- `getCarStatusStatistics()` - статистика по статусам
- `getDailyRevenue()` - доход по дням

---

## 📁 Структура файлов:

```
patterns/
├── pricingstrategy.h/cpp      - Паттерн Strategy
└── carstatusobserver.h/cpp     - Паттерн Observer

services/
├── rentalservice.h/cpp         - Бизнес-логика аренды
└── pricingcalculator.h/cpp    - Калькулятор стоимости

managers/
└── reportmanager.h/cpp         - Менеджер отчетов
```

---

## 🔄 Примеры использования:

### Пример 1: Расчет стоимости аренды
```cpp
PricingCalculator calculator;
Car car = db.getCarById(1);
QDate start(2025, 1, 15);
QDate end(2025, 1, 20);

// Автоматический выбор стратегии
calculator.selectOptimalStrategy(start, end);

// Расчет стоимости
double cost = calculator.calculateRentalCost(car, start, end);
// Результат: 12500 руб (5 дней × 2500)
```

### Пример 2: Создание аренды с автоматическим обновлением статуса
```cpp
RentalService service;
service.createRental(carId, userId, startDate, endDate, cost);
// Автоматически:
// 1. Проверяется доступность автомобиля
// 2. Создается аренда в БД
// 3. Статус автомобиля меняется на "В аренде" (Observer)
```

### Пример 3: Расчет штрафа
```cpp
RentalService service;
QDate actualReturn(2025, 1, 22); // Просрочка на 2 дня
Fine fine = service.calculateFine(rentalId, actualReturn, 1.5);
// Результат: 7500 руб (2500 × 1.5 × 2)
```

### Пример 4: Генерация отчета
```cpp
ReportManager reportManager;
QDate start(2025, 1, 1);
QDate end(2025, 1, 31);
RevenueReport report = reportManager.generateRevenueReport(start, end);
// Получаем полную статистику за месяц
```

---

## ✅ Требования выполнены:

- ✅ **Паттерн Strategy** - реализован для расчета стоимости
- ✅ **Паттерн Observer** - реализован для обновления статуса
- ✅ **Бизнес-логика** - созданы сервисы для работы с арендой
- ✅ **Расчет стоимости** - поддержка разных стратегий
- ✅ **Расчет штрафов** - автоматический расчет просрочки
- ✅ **Отчеты** - система генерации отчетов

---

## 🚀 Следующий этап:

**Этап 3: Интерфейс пользователя**
- Окно входа
- Главное окно клиента
- Главное окно администратора
- Диалоги для работы с данными

